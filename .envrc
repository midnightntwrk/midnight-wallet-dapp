# point out to users at commit time that commits need to be signed,
# not once they've done many commits and are trying to push a PR:
git config --local commit.gpgSign true
git config --local tag.gpgSign true

export COMPACTC_VERSION=0.31.0 # must be set to a valid version of compactc
export COMPACT_TAG_PREFIX=compactc-v # override default value
export COMPACT_REPO=midnightntwrk/compact # override default value

export PROOF_SERVER_VERSION=8.0.3
export INDEXER_VERSION=4.0.1
export MIDNIGHT_NODE_VERSION=0.22.5

echo "COMPACTC_VERSION=${COMPACTC_VERSION}"
echo "COMPACT_TAG_PREFIX=${COMPACT_TAG_PREFIX}"
echo "COMPACT_REPO=${COMPACT_REPO}"
echo "PROOF_SERVER_VERSION=${PROOF_SERVER_VERSION}"
echo "INDEXER_VERSION=${INDEXER_VERSION}"
echo "MIDNIGHT_NODE_VERSION=${MIDNIGHT_NODE_VERSION}"
